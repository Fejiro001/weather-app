import logo from "../assets/images/logo.svg";

const Logo = () => {
  return (
    <div>
      <img className="h-10 w-auto" alt="Weather Now Logo" src={logo} />
    </div>
  );
};

export default Logo;
